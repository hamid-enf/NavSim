%MAIN Launch the interactive Navigation Simulator GUI.
%   >> cd NavSim
%   >> main
%
% For headless runs / tests see runAllTests.m and docs/USER_GUIDE.md.
startup();
app = NavSimApp();   %#ok<NASGU>  % keep a reference so the app stays alive
